name: Typecheck

on:
  push:
    branches: [ main, dev ]
  pull_request:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Pixi
        uses: prefix-dev/setup-pixi@v0.8.3
        with:
          manifest-path: backend/pyproject.toml

      - name: Run Lefthook CI group (Pixi env)
        run: |
          # lockfile-precise install+run:
          pixi run --manifest-path backend/pyproject.toml typecheck
