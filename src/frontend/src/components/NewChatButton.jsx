export default function NewChatButton() {
  return (
    <button
      className="!bg-white text-[#038061] px-3 py-1 rounded"
      onClick={() => {}}
      aria-label="New Chat"
    >
      Current Chat Name
    </button>
  );
}
