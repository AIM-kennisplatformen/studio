# Studio frontend

React and Vite frontend for Studio's chat and knowledge graph exploration.

## Development

```bash
npm ci
npm run dev
```

Open `http://localhost:5174`. The development server proxies `/api` and
Socket.IO traffic to `http://localhost:10092` by default.

## Checks

```bash
npm run check
npm run build
```
